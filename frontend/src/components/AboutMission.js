import React from "react";

export default function AboutMission() {
  return (
    <section className="px-4 py-8 max-w-5xl mx-auto text-center">
      <h2 className="text-3xl font-bold mb-4 text-primary">Our Mission</h2>
      <p className="text-base text-base-content/70">
        We believe that choosing where to eat should be easy and inclusive. ForkIt lets groups vote, rate, and receive intelligent suggestions so no one is left out of the decision.
      </p>
    </section>
  );
}
